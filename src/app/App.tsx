import { RouterProvider } from 'react-router';
import { router } from './routes';

// Image Compression System — CENG 384 Group 10
export default function App() {
  return <RouterProvider router={router} />;
}
